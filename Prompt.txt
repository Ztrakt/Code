That fixed it. Now i'm going back to address some things. 1) I see the created meshes don't have the same origin point as before, it goes directly to the pivot. Does it affect the animation? 2) The BONE_#_RIGID_MODEL_# empties bones of the skinned ones do exist. The empties are animated because they have it as a parent bone with Tracking Axis +Y and Up Axis Z (blender). Taking this into account might fix some broken animations. 3) Every joint seem to have a bool to indicate whether it inherits scale or rotation from parent or both, so it shouldn't be not inheriting by default BUT they should be baked because their value differ in animations. In the JointSettings, they show as “IndependentScaling” and “UseAdditionalRotation”
